# Multi-plugin assembly sample

Contains two `PluginBase` types in one source package. Each contributes a prompt command that returns a visible status message, and both plugin types share one dynamic load unit and unload together.
