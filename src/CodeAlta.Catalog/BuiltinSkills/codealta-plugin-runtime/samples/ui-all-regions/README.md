# UI all-regions sample

Contributes a visual marker for every `PluginUiRegion`, thread status/footer status items, renderer callbacks for attachment/timeline/tool-call rendering, and a `/ui-sample` command that shows a notification/status message. Copy this sample into a plugin root to verify which UI regions are currently wired into the live UI.
