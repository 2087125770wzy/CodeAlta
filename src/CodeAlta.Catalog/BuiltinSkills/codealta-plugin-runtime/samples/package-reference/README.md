# Package reference sample

Uses `#:package Humanizer` to verify plugin-private package restore/copy/loading. The `/sample-humanize` command returns a visible message produced through the package. Package restore can run third-party build logic; enable only trusted sources.
