# Hello command sample

Adds a low-risk `/hello` prompt command. Running it writes a visible status message so you can tell that plugin commands are loaded and executing. Enabling it still executes trusted plugin code inside CodeAlta.
